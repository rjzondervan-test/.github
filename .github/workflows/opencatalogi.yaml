name: My PublicCode Workflow

on:
  push:
    branches:
      - main

permissions: write-all

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Update publiccode.yaml
        uses: OpenCatalogi/publiccode-action@235a9199fb41a7c9acc21c1894ee7705ac97b938
